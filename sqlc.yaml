version: "2"
sql:
  - engine: "postgresql"
    queries: "database/user_queries.sql" # Where your SQL queries are
    schema: "database/user.sql" # Where your CREATE TABLE statements are
    gen:
      go:
        package: "postgresql"       # The package name for generated code
        out: "internal/interface/postgresql"           # Output directory
        sql_package: "pgx/v5" # CRITICAL: Use pgx native types
        emit_interface: true